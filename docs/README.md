# Public reports

Authored notes for this repository. They are written as eventually public:
Markdown and LaTeX sources, plus the PDFs built from them.

No report has been added here yet. When one is, give it its own directory
holding the source, `references.bib`, `figures/` and a `Makefile` that
rebuilds the PDF, then list it in the table below.

| Report | Format |
| --- | --- |
| _(none yet)_ | |

The generated project website is a different tree, `.github/docs/`. It is
built by `.github/scripts/build.sh` from the literate-C and Python sources and
published to GitHub Pages by `.github/workflows/deploy.yml`. Do not hand-edit
it; change the generator instead.
